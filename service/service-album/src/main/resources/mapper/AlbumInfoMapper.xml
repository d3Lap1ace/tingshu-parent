<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >


<mapper namespace="com.impower.tingshu.album.mapper.AlbumInfoMapper">


    <select id="getUserAlbumPage" resultType="com.impower.tingshu.vo.album.AlbumListVo">
        select ai.id albumId,
               ai.album_title,
               ai.cover_url,
               ai.include_track_count,
               ai.is_finished,
               ai.status,
               max(if(stat.stat_type='0401',stat.stat_num,0)) palyStaNum,
               max(if(stat.stat_type='0402',stat.stat_num,0)) subscribeStatNum,
               max(if(stat.stat_type='0403',stat.stat_num,0)) buyStatNum,
               max(if(stat.stat_type='0404',stat.stat_num,0)) commentStatNum
        from album_info ai
                 left join album_stat stat on stat.album_id = ai.id
        <where>
            ai.user_id = #{userId} and ai.is_deleted = 0
            <if test="albumInfoQuery.status != null and albumInfoQuery.status != ''">
                and ai.status = #{albumInfoQuery.status}
            </if>
            <if test="albumInfoQuery.albumTitle != null and albumInfoQuery.albumTitle != ''">
                and ai.alblum_titile like concat('%',#{albumInfoQuery.albumTitle},'%')
            </if>
        </where>
        group by ai.id
        order by ai.id desc
    </select>
</mapper>

